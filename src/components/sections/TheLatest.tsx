import SectionContainer from "../SectionContainer";
import SectionHeading from "../SectionHeading";

function TheLatest() {
  return (
    <SectionContainer className="bg-mint-100">
      <SectionHeading>the latest</SectionHeading>
      <div>list</div>
      <div>random thumbnail</div>
      <div>featured authors</div>
    </SectionContainer>
  );
}

export default TheLatest;
